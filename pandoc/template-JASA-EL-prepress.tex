% !TEX TS-program = xelatex
% !TEX encoding = UTF-8
\documentclass[11pt,oneside]{article}
\usepackage[letterpaper, left=120pt, right=120pt, top=50pt, bottom=50pt]{geometry}
$if(natbib)$\usepackage[super,sort&compress,comma]{natbib}\bibliographystyle{$if(biblio-style)$$biblio-style$$else$plainnat$endif$}$endif$
$if(listings)$\usepackage{listings}$endif$
$if(tables)$\usepackage{tabularx,array,booktabs,multirow}$endif$
% use microtype if available
\IfFileExists{microtype.sty}{\usepackage{microtype}}{}

$if(graphics)$
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
% Scale images if necessary, so that they will not overflow the page
% margins by default, and it is still possible to overwrite the defaults
% using explicit options in \includegraphics[width, height, ...]{}
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
$endif$

\usepackage{hyperref}
\hypersetup{breaklinks=true,
            bookmarks=true,
            pdfauthor={$author-meta$},
            pdftitle={$title-meta$},
            colorlinks=false,
            pdfborder={0 0 0}}

% % % % % % % % % % % % % % % % % %
% MOSTLY JASA-SPECIFIC FORMATTING %
% % % % % % % % % % % % % % % % % %
\usepackage[reqno]{amsmath}
\usepackage{setspace}
% TITLE PAGE HACKS
\usepackage{titling}
\pretitle{\begin{center}\LARGE\bfseries}
\posttitle{\par\end{center}\smallskip}
\preauthor{\begin{center}\begin{tabular}[t]{c}}
\postauthor{\end{tabular}\par\end{center}}
\predate{\vspace{-\baselineskip}}
\postdate{\vspace{-\baselineskip}}
% formatting for the titlepage footnotes
\thanksmarkseries{alph}
\thanksheadextra{\thinspace}{)}
\thanksfootextra{}{)}
\setlength{\thanksmarkwidth}{1em}
\setlength{\thanksmargin}{0em}
\setlength{\footnotesep}{2em}
% author block
\usepackage[blocks]{authblk}
\renewcommand{\Authfont}{\normalsize\bfseries} 
\renewcommand\Affilfont{\normalfont\small\itshape} 
\setlength{\affilsep}{0in}
% caption size
\usepackage[font=small]{caption}
%% push all textual footnotes to the end and print them normal size
%\usepackage{endnotes}
%\let\footnote=\endnote
%\def\enotesize{\normalsize}
%\renewcommand\enoteformat{\noindent\makebox[1em][l]{\bfseries\theenmark}\hangindent 1em}
%% remove extra space between "NOTES" and first endnote (caused by previous command)
%\apptocmd{\enoteheading}{\vspace{-\baselineskip}}{}{}
%% format endnotes name to match other headings
%\renewcommand{\notesname}{\bfseries\large\uppercase{notes}}
% formatting for section & subsection headings and table captions
\usepackage{titlesec}
%\titleformat{command}[shape]{format}{label}{sep}{beforecode}[aftercode]
\titleformat{\section}[hang]{\bfseries}{\thesection}{0.5em}{}%
\titleformat{\subsection}[hang]{\itshape}{\thesubsection}{0.5em}{}%
%\titlespacing*{command}{left}{before}{after}[right]%
\titlespacing*{\section}{0pt}{0.4\baselineskip}{0.2\baselineskip}%
\titlespacing*{\subsection}{0pt}{0.4\baselineskip}{0.5em}%

\renewcommand{\thesection}{\arabic{section}.} 
\renewcommand{\thesubsection}{\arabic{section}.\arabic{subsection}} 
\renewcommand{\thesubsubsection}{\arabic{section}.\arabic{subsection}.\arabic{subsubsection}}
\renewcommand*\thetable{\Roman{Table}}
% format various headings to match the style of section headings 
\renewcommand{\refname}{\bfseries{References}}
\renewcommand{\listfigurename}{\bfseries{List of Figures}}
% bibliography spacing
\def\bibfont{\small}
\setlength{\bibindent}{0em}
\setlength{\bibsep}{0pt plus 0.3ex}
\makeatletter
\renewcommand\@biblabel[1]{\textsuperscript{#1}}
\makeatother
\setcitestyle{aysep={}}
% % % % % % % % % % % % % 
% other customizations  %
% % % % % % % % % % % % % 
%% VULGAR FRACTIONS
\usepackage{xfrac}
% packages that attempt to move all tables and figures to the end
%\usepackage[blank, printfigures]{figcaps}
%\usepackage[tablesfirst, notablist, figlist]{endfloat}
% citation alias to avoid an in-text citation to the overlong "American National Standards Institute"
\defcitealias{ansi2004}{ANSI, 2004}
\usepackage{placeins}  % provides \FloatBarrier, inserted by the pandoc postprocessor 
%\patchcmd{\thanks}{#1}{\protect\singlespacing\normalsize#1}{}{}
\usepackage{fontspec}
\setmainfont[Ligatures=TeX, Numbers={Lining}]{Times New Roman} 
\setmonofont{Arial}
\renewcommand{\url}{\begingroup \def\UrlLeft{}\def\UrlRight{}\urlstyle{same}\Url} % set URLs in whatever font surrounding text uses
\thickmuskip=5mu plus 3mu minus 1mu % adjust equation spacing around =

% % % % % % % % % % % % % % % % %
% title, subtitle, author, date %
% % % % % % % % % % % % % % % % %
$if(title)$\title{$title$$if(titlenote)$\thanks{$titlenote$}$endif$}$endif$
$if(subtitle)$\subtitle{$subtitle$}$endif$

$for(author)$\author{$author.name$}
$if(author.affiliation)$\affil{$for(author.affiliation)$$author.affiliation$$sep$ \& $endfor$}$endif$
$endfor$
\affil{$for(author)$$author.email$$sep$, $endfor$}
$if(date)$\date{$date$}$else$\date{}$endif$
\begin{document}

$if(title)$\maketitle$endif$ %$if(runningtitle)$\begin{center}Running title: $runningtitle$\end{center}$endif$
$if(abstract)$\begin{quotation}\noindent\textbf{Abstract:} $abstract$\newline{\footnotesize © 2015 Acoustical Society of America}$if(keywords)$\newline{\footnotesize Keywords: $for(keywords)$$keywords$$sep$, $endfor$}$endif$\end{quotation}$endif$ %$if(pacs)$\bigskip PACS Numbers: $for(pacs)$$pacs$$sep$, $endfor$$endif$


$for(include-before)$
$include-before$
$endfor$

$body$

$if(acknowledgments)$\section*{acknowledgments}$acknowledgments$\cleardoublepage$endif$

%% ENDNOTES
%\begingroup
%\parindent 0pt
%\parskip 1em
%\theendnotes
%\endgroup
%\cleardoublepage

$if(natbib)$
$if(biblio-files)$
$if(biblio-title)$\renewcommand\refname{$biblio-title$}$endif$
\bibliography{$biblio-files$}
$endif$
$endif$

$for(include-after)$
$include-after$
$endfor$

%\end{spacing}
\end{document}
